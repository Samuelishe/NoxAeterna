# Next Steps

The app now has the first real dark/light theme switching path, JSON-backed UI localization, a thin localized shell, and a temporary debug preview for the chart pipeline.

Immediate next steps:

1. Start replacing the temporary debug preview infrastructure with the first real astrology workspace foundation while keeping the renderer isolated.

2. Keep settings persistence deferred until the workspace structure and preference flow are stable.

3. Preserve an easy visual verification route for the chart pipeline while the real astrology workspace replaces the temporary debug section.

4. Verify Swiss Ephemeris wrapper options before committing to a package and record the decision in `DECISIONS-LOG.md`.

5. Keep real profile entry, houses, advanced collision avoidance, and interpretation localization out of scope until the first real astrology workspace foundation exists.
